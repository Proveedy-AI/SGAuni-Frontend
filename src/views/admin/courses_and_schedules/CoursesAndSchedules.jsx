export const CoursesAndSchedules = () => {
  //const { data: dataCourses, isLoading: isLoadingCourses } = useReadCourses();
  //const { data: dataSchedules, isLoading: isLoadingSchedules } = useReadSchedules();

  return (
    <div>
      <h1>Cursos y Horarios</h1>
    </div>
  )
}